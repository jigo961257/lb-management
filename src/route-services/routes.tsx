import { API_URL } from "@/constants/Constant";

export const routes = {
    generic: {
        GET_KNOWN_LANGUAGE: {
            url: API_URL + "/generic/get-languages",
            title: "get known language"
        }

    }

}